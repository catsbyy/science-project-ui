import { Option } from './Option';

export interface Response {
  regions: Option[];
  techAndTools: Option[];
  english: Option[];
  education: Option[];
  position: Option[];
  salary: Option[];
  workArea: Option[];
  workExp: Option[];
  workplace: Option[];
}